<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@android:color/white"
    android:layout_marginBottom="12dp">

    <ImageView
        android:id="@+id/imgCover"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:scaleType="centerCrop"
        android:layout_marginEnd="12dp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:orientation="vertical"

        android:gravity="center_vertical">

        <com.example.mylibrary.ds.text.DsText
            android:id="@+id/tvTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:dsTextStyle="subtitle"
            app:dsFontWeight="bold"
            android:textSize="14dp"
            android:text="Mercado financeiro reage positivamente" />


        <com.example.mylibrary.ds.text.DsText
            android:id="@+id/tvDescription"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:dsTextStyle="text"
            android:maxLines="2"
            android:ellipsize="end"
            android:text="Investidores mostram otimismo com as novas políticas ..." />


        <com.example.mylibrary.ds.text.DsText
            android:id="@+id/tvDate"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:dsTextStyle="text"
            android:textColor="#888888"
            android:text="Hoje, 09:15" />
    </LinearLayout>
</LinearLayout>
